# Coco: punto de continuación y candidato de cierre

Estado: desarrollo detenido por petición del usuario tras E07.n2b. Cierre/publicación de `2.42.0-next.1` en curso; comprobar el registro de publicación al final antes de afirmar que está desplegado. Canal npm `next`, sin mover `latest`; no publicar VSIX ni hosted. Rama `codex/coco-evolution`; último commit de producto: `435aae7`.

## Decisiones que se mantienen

Coco es un agente de programación CLI/REPL en TypeScript. Sin migración Python/FastAPI/LangGraph ni plataforma general. Conservar catálogo de modelos; Gemini SDK2.22.0 es la actualización aplicada. Otras API/modelos se revisan en E08/E13 con documentación oficial vigente. No se hicieron inferencias pagadas ni se acreditó mejora de resolución de tareas con un modelo real.

La implementación completa se estimó aproximadamente40% realizada por alcance, no por commits ni certificación. No usar ese porcentaje para decidir una release: importan los bloqueos de cada superficie. El usuario ha pedido guardar lo hecho y continuar otro día. No reanudar automáticamente ni crear tareas periódicas.

## Qué está terminado

El plan vigente es [COCO_EVOLUTION_PLAN](../COCO_EVOLUTION_PLAN.md); sus criterios permanecen vigentes. El [registro E07](E07_CANCELLATION.md) recoge cada incremento, fuentes, pruebas y revisión independiente. Los revisores son subagentes separados del implementador, del mismo servicio; no consultores humanos externos ni evaluación multivendedor.

- E01: baseline hermético y corpus fixture; inferencia real pendiente.
- E02: infraestructura de release/npm y smoke de paquete; publicación remota se verifica durante este cierre. VSIX bloqueado hasta E11.
- E03–E05: frontera compartida de herramientas, permisos/rutas/shell, autoridad MCP y delegación. Undo de archivos y flujos clásicos incompletos contenidos explícitamente.
- E06.a–g: contenciones de falsos éxitos, demos y aceptación; E06 permanece abierto por disponibilidad/aplicabilidad de mediciones.
- E07.a–k3: propiedad de subprocesses, retry cancelable, señales y scopes de proveedores/auth, renovación compartida Copilot. Los registros delimitan qué pruebas son reales y cuáles simuladas.
- E07.l1–l5b: lifecycle de requests y transports MCP, OAuth, cierre y reconexión SSE, stdio con TERM/KILL y espera de close, HTTP drena envíos; stop del manager conserva referencias hasta cierre.
- E07.m1–m3: manager/executor delegado y graph/workflow cancelables, sin éxito tardío; resultados parciales y diagnóstico preservados.
- E07.n1: JSON de tools completo y objeto, sin reparación/{} inventado, error no recuperable ni payload sensible en logs.
- E07.n2a: terminal válido OpenAI CC/Responses, incluidos consumidores heredados como Copilot; integridad de IDs/argumentos; lote validado antes de emitir tools. Gate amplio356 archivos/7790 PASS,15 skipped; REPL27; tipos/lint/formato/build PASS.
- E07.n2b: misma garantía para tools Anthropic con su protocolo de bloques/message_stop; gate154 PASS,24 contratos terminales, tipos/lint/formato PASS. El gate de release posterior sustituye estos números como última comprobación global.

Commits recientes: `4623fd1` SSE, `e70a6f5` stdio, `458ae47` lifecycle HTTP, `a0a99f6` argumentos, `7da76cb` terminal OpenAI, `435aae7` terminal Anthropic. Git contiene la secuencia completa, sin macrocommit que la sustituya.

## Siguiente trabajo, en orden

1. **E07.n2c: Codex tools.** `src/providers/codex.ts`: retener arguments.done hasta response.completed válido, rechazar incomplete/failed/EOF, validar todo el lote antes de tool_use_end; reutilizar ResponsesToolCallAssembler.finalizeCompleted, conservar IDs distintos y rechazar contradicciones. Cubrir chatWithTools y streamWithTools, completed.output sin granular, cancelación y no replay. El backend ChatGPT no se declara API pública estable.
2. **E07.n2d: Gemini/Vertex tools.** `src/providers/{gemini,vertex}.ts`: no convertir MAX_TOKENS/bloqueo/EOF en tool_use porque aparezcan funciones. Validar terminal nativo, inputs completos y firmas Gemini; ninguna ejecución provisional. Contrastar documentación oficial.
3. **Consumidor y cancelación restantes.** `src/cli/repl/agent-loop.ts`: no aceptar tools incompletas ni defaults{} del consumidor; exigir final coherente antes de efectos. Revisar coordinator/sprint-runner (timeout de CoordinationOptions no aplicado, señal no propagada) y callbacks onboarding. No ampliar a reconstrucción del orquestador.
4. **Procesos y cuotas.** `src/tools/bash.ts`: limitar stdout/stderr DURANTE lectura, preview head/tail y disco acotados; el truncado posterior no limita memoria. Probar child/descendant con marcador tardío tras cancelar. Background detached/unref requiere ownership real o contención explícita; no usar kill global por nombre. MCP frames/buffers requieren límites. No afirmar cancelación remota ni sandbox.
5. **Contención de calidad para Release B estable.** `src/tools/quality.ts`, `src/tools/index.ts`, analizadores: calculate_quality sigue anunciando medición real mientras disponibilidad/aplicabilidad y overrides requieren E09. Ausencia de herramienta/configuración nunca debe producir aceptación medida. Corregir o desactivar explícitamente esa certificación, manteniendo autoinforme etiquetado. No repetir correcciones E06.d/g ya hechas.
6. **Gate Release B estable.** P0/P1 del artefacto resueltos o contenidos y documentados, paquete instalado del hash exacto, cobertura/pisos sin manipular, revisión independiente y reconciliación npm. E08 completo, E10 avanzado, E11 VSIX, E12 UX y E13 no son prerrequisitos de la primera mejora estable si los riesgos de la superficie están contenidos.
7. **Continuar E08–E15 después.** Schemas fieles y matriz modelo/API; mediciones y aceptación del hash final; sesión/rewind sin perder staged/untracked; headless y VSIX; fluidez del terminal; contexto/prompts/modelos evaluados; recorrido instalado/deuda/cobertura80%; auditoría final con agentes de contexto limpio. E15 sigue obligatoria antes de anunciar terminado todo el programa, pero no bloquea candidatos intermedios.

## Cómo retomar sin repetir la auditoría

Leer AGENTS.md y CLAUDE.md, este documento, la sección9 del plan y últimas entradas E07. Ejecutar git status/log y comprobar versión/rama/remoto antes de editar. No resetear ni pisar cambios ajenos. Activar únicamente el siguiente subpaso concreto; implementar → pruebas significativas → revisión por agente independiente → corregir → commit convencional → actualizar progreso. No expandir el alcance por refactors cosméticos.

Los tests de este programa se ejecutan en copia hermética, nunca directamente en el checkout personal: hubo fixtures históricos con efectos sobre rutas reales. En esta máquina, `/tmp/coco-evolution-location` apunta al entorno Node22/mirror, `/tmp/coco-check-increment.py` sincroniza cambios y ejecuta checks bajo sandbox (sin credenciales reales ni red externa; loopback permitido). No sobrescribir HOME. Scripts temporales pueden desaparecer: si faltan, recrear copia limpia aislada siguiendo E01 antes de probar. Logs conservados bajo `.dev/evolution/e01-baseline/logs` (ignorados por Git) y en Actions para la release.

Comandos de aceptación: `pnpm check:release` (incluye cobertura, REPL y build); `npm pack --ignore-scripts`; instalar tarball en consumidor temporal y `node scripts/smoke-package.mjs <consumer> <version>`. Para publicar usar workflow Release/tag correcto, que selecciona next para prereleases y compara integridad remota. No cambiar dist-tags estables ni marcar deploy correcto solo por haber empujado un tag.

## Registro de publicación

Gate local `pnpm check:release`: PASS. 357 archivos,7814 pruebas correctas,15 omitidas; REPL27; typecheck/lint/format/build PASS. Cobertura statements70.47%, branches62.63%, functions76.29%, lines71.11%; se mantienen los pisos anteriores, no se afirma cumplir el objetivo80%.

Candidato `2.42.0-next.1`, tag previsto `v2.42.0-next.1`. Pendientes de completar: commit/tag remotos, URL Actions, integridad tarball/npm, smoke instalado, URL GitHub release y dist-tags comprobados. Si falla, registrar el bloqueo real y no declarar publicación.
